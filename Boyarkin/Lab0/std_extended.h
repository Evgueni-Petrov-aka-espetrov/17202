#include <cmath>
#include <string>
#include <regex>

using namespace std;

namespace std_extended {
	long long GCD(long long a, long long b);
	long long LCD(long long a, long long b);
	int CharToInt(char digit);
	char IntToChar(int digit, bool upperRes = true);
}